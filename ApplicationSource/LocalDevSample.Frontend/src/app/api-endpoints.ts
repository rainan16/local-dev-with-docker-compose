const baseApiUrl = window.browserEnvironment.baseApiUrl;
const serviceA = window.browserEnvironment.serviceaApiUrl;
const serviceB = window.browserEnvironment.servicebApiUrl;

export const SERVICEA = `${serviceA}`;
export const SERVICEB = `${serviceB}`;
